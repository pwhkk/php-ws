{
  "name": "node-websocket-server",
  "version": "1.0.0",
  "description": "HTTP + WebSocket server with runtime config file support (Linux only)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:linux": "pkg index.js --targets node18-linux-x64 --output app-linux"
  },
  "author": "YourName",
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}